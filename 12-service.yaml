apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    # labels are used as delectors
    project: roboshop
    component: frontend
    environment: dev
  ports:
  - protocol: TCP
    port: 80 #sevice port (internal cluster IP)
    targetPort: 80   # container port (must match your Pod's containerPort)